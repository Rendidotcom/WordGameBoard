{
  "version": 2,
  "builds": [
    { "src": "backend/src/api/**/*.ts", "use": "@vercel/node" },
    { "src": "frontend/**",            "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/src/api/$1.ts" },
    { "src": "/(.*)",     "dest": "frontend/$1" }
  ]
}
